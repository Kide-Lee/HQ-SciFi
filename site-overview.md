# 荒启网站现状与浏览器实测记录

> 探索日期：2026-08-06。方法：通过 browser-harness（CDP 连接 Helium 浏览器）实际浏览 `www.huangqisf.com` 主站与 `www.huangqisf.com/h5/` 平台，抓取 DOM 与页面文本。所有结论均为浏览器实弹验证。
> 前置知识：API 层调查见 [api-research.md](api-research.md)（基于 h5 前端包静态分析 + 直接请求实测）。

## 1. 站点双轨结构总览（重要）

荒启科幻目前是**两个面共用一个品牌/域名**，不要混淆：

| 面 | 地址 | 性质 | 技术栈 |
| --- | --- | --- | --- |
| **AI 科幻文学分析库**（主站） | `https://www.huangqisf.com/`（首页/`/works`/`#download`/`#contact`） | 内容展示站：AI 生成的科幻小说结构化分析 | **Astro 静态站**（`/_astro/*.js`、`/_astro/*.css`），数据构建期预渲染 |
| **写作社区平台**（H5） | `https://www.huangqisf.com/h5/#/` | 用户创作平台：投稿/练笔/评审/连载/作品库 | SPA（`/h5/` 包，`api.huangqisf.com` 后端），即 api-research.md 调查的对象 |

- 页脚统一署名：**成都荒启文化传播有限公司** · © 2026 荒启科幻 · 蜀ICP备2025169184号-1 · QQ 群 280660235。
- **主站页脚明确声明「本网站的所有内容均为 AI 生成」**——分析库全部内容（含作品分析）由 AI 产出。

## 2. 主站：AI 科幻文学分析库

### 2.1 定位文案（首页原话）

> 汇集 5 个赛事期次的 97 篇已复核科幻小说，提供结构化叙事、科幻设定与批评细读，供读者按期次与主题探索作品版图。

### 2.2 期次与作品统计（实测）

- **97 篇已复核作品**，按期次（即 h5 练笔活动期次）组织：

| 期次 | 专题 | 篇数 |
| --- | --- | --- |
| 第 13 期 | 机甲 | 22 |
| 第 12 期 | 越界者 | 21 |
| 第 11 期 | 文明重启 | 21 |
| 第 10 期 | 时间裂缝 | 24 |
| 第 1 期 | 雨季 | 9 |

- 主题专题（首页轮播 5 屏）：机甲 · 越界者 · 文明重启 · 时间裂缝 · 雨季。
- **科幻子类型**（/works 过滤项，共 18 个）：近未来社会、乌托邦/反乌托邦、灾难、基因/生物、AI/人工智能、元科幻、太空歌剧、军事科幻、赛博朋克、外星接触、末日/废土、时间旅行、其他、推理科幻、虚拟现实、奇幻边界、蒸汽朋克、太阳朋克。

### 2.3 URL 与页面路由

- 作品列表：`https://www.huangqisf.com/works`（SPA 过滤：期次多选 + 子类型多选，均通过 `?phase=` 与复选框客户端过滤，共 97 篇）
- 作品详情：`https://www.huangqisf.com/works/<期次>/<slug>`（如 `/works/12/方舟`；slug 为作品标题，URL 编码，**路径尾有 `/`**）
- 每篇详情页是**独立静态页**（Astro 预渲染，无 SPA 数据请求）。

### 2.4 作品详情页结构（核心资产）

单页四板块 + 两个折叠区（左侧目录按钮切换：叙事 / 科幻设定 / 批评 / 形式）：

1. **叙事**：叙事模式（顺叙…）、时间结构、聚焦（限知…）、叙述可靠性、情节弧类型；**人物表**（姓名/角色/特征/引文）；**设定表**（名称/类型/引文）。
2. **科幻设定**：主题命题（2–3 条可争论命题）、标签、陌生化（评级：高/…）、**核心设定项**（每个 H4：名称 + 一句话机制 + 呈现方式[渐进揭示/场景呈现/说明灌输] + 引文）、互文（目标/类型/引文）。
3. **批评**：总评（一段，同时被用作 `<meta name="description">`）、优点（编号要点，引用 L 行号）、薄弱点（编号要点）、细读（按关键段落 L1–xx 分段精读）。
4. **形式**（折叠）：对话占比（如 68%）、平均句长（如 19.8 字）、场景数、人称分布（第一/第二/第三人称百分比）。
5. **横向比较**（折叠）：与同期次其他作品的 AI 横向对比。

- 分析文本大量使用内部 schema 标记：`L25`（行号引用）、`dialogue_ratio≈0.68`、`sf.estrangement_rationale`、`phase_theme_response=explicit`、`narrative.focalization=limited`、`intro_mode=infodump/dramatized`、`C02/B07`（设定/设置编号）等——说明分析由带结构化 prompt 的 LLM 流水线生成。
- 作品卡片（列表页）显示：标题、期次、字数（约 X 字）、子类型标签、总评摘要（截断）。

### 2.5 数据可达性（对客户端重要）

- **主站无公开 JSON API**：Astro 静态站，数据在构建期写入 HTML/JS；`performance` 资源里只有 `/_astro/*` 静态资源。
- 若客户端要消费分析库数据，只能**爬静态 HTML 页面**（97 个详情页 + 列表页）自行建索引，或与站方协商数据导出。页面 `meta description` 即总评，利于抓取摘要。

## 3. 官方客户端下载（首页下载区）

- **Android**：`https://www.huangqisf.com/dt/Huangqisf.apk`
- **Windows**：`https://www.huangqisf.com/dt/荒启科幻.exe`（URL 编码 `%E8%8D%92%E5%90%AF%E7%A7%91%E5%B9%BB.exe`）
- **iOS**：`#download` 区有入口（未实测链接）
- **H5**：`https://www.huangqisf.com/h5/`

> 注意：官方已有 Windows 客户端（.exe）。hqsf-client 的定位是独立/第三方兼容客户端，需与官方客户端区分（功能、品牌、分发路径）。

## 4. H5 写作社区平台现状

- 入口：`https://www.huangqisf.com/h5/#/`；底部 tab：首页 / 发现 / 消息 / 我的；首页内容 tab：推荐 / 连载 / 活动 / 作品库（另有 文章/帖子/动态/商品 分类）。
- 首页推荐流出现「万象朋克悬赏」征稿活动（如《本文探讨了以仙侠朋克为主的一系列朋克有关内容》等投稿）。
- **练笔活动**（`/h5/#/pages/contents/allActive`）实测完整列表：**荒启科幻练笔第 1–24 期**，其中：
  - 进行中：第 24 期
  - 评审中：第 23 期
  - 已结束：第 1–22 期（练笔）+ 「未来校园档案」征文 + 第二届群星杯征文 + 荒启科幻第一届NTR创作活动
- **与主站分析库的对应关系**：分析库的 5 个期次（1/10/11/12/13）就是练笔活动的第 1/10/11/12/13 期——分析库 = 从 24 期练笔中精选 5 个期次的 97 篇「已复核」作品做 AI 深读。
- api-research.md 的接口调查（contentsList/review/metas 等）仍适用于本平台；正文需登录（`contentsInfo` 带 token）——api-research.md §9.4。

## 5. 对 hqsf-client 的影响与待确认

1. **M2「文章列表/详情阅读」的新可能**：官方已上线「AI 分析库」形态的阅读内容（结构化叙事/设定/批评）。但主站是静态站、无 API；h5 平台正文仍需 token。客户端阅读视图的取舍：接 h5 `contentsInfo`（需登录）or 爬分析库静态页（公开但非官方 API）。
2. **评审面板**：h5 评审体系（review/*）与分析库的「批评」板块是两套东西——前者是社区评审接口（api-research.md §4），后者是站方 AI 分析产物，不可混用。
3. **官方已有 Windows/Android 客户端**：确认 hqsf-client 差异化定位（如本地 md 编辑/同步、评审工作流）。
4. **待确认**：分析库详情页是否持续新增（当前 97 篇，覆盖 5 期；后续期次是否会补分析）；官方客户端与 h5 是否同一账号体系（大概率是，api-research.md 的 userLogin 体系）。
