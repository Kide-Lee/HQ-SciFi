# 更新日志

## v0.0.5（开发中，未发布）

### 修复
- **右栏不再出现水平滚动条**：评论/评审 tab 在窄栏（最小 219px）下内容自适应收缩
  - 根因：评论区遗留「正文下方」样式的 `margin:auto` 居中在右栏 flex 布局下把宽度收缩到内容 min-content；楼中楼子评论窄容器下头像+时间+回复按钮固定宽度总和超容器；评审表单 range 固有 min-width 撑破窄栏
  - 修复：右栏 flex 链加 `min-width:0`，评论昵称/时间窄栏下省略号收缩，评审 range 允许收缩
- **评审评论数未透传**：「查看评审评论」按钮在有评论的评审上不显示——`toReviewItem` 转换时丢弃了服务端返回的 `replyNum` 字段，补上透传（如《天空高远，四野昏黄》流火评审 replyNum=1，现在正确显示按钮与数量）

### 改进
- **评审维度卡片改为上下排布**：五维（设定/文笔/人物/情节/思想性）与综合评价每项一行，维度名在上（加粗小标题样式，去灰底）、评语在下；维度间距 8px
- **维度名显示此项分数**：维度名后跟该项评分（取自 `review.score` 五维数组，accent 色），无评分（`-.-`）不显示；维度名字号 16px，大于评语正文（15px）
- **评审卡片头部两行布局**：评审者名字下方新增评审发布时间一行；头像放大到 32px、评分字号 16px/700 加粗以适应两行结构
- **评论卡片头部同步两行布局**：昵称下方新增评论时间一行（去掉原「· 」前缀），头像放大到 32px；「回复」按钮保持原大小
- **评审态度按钮 emoji 化**：开心/有用/认真改为表情反应（😄/👍/🧐 + 计数，Discord「添加反应」风格）；「回复评审」「查看评审评论」改为纯图标按钮，「查看评审评论」后显示评论数（`review.replyNum`），数量为 0 时隐藏该按钮；操作区窄栏下可换行
- **评审评论移入评审卡片**：评论 tab 不再展示关联评审的评论（剔除 reviewid>0 的评论及分组）；评审卡片内嵌评论区（默认收起，点「评论 N」展开/收起，含回复框与评论列表，可楼中楼回复），「回复评审」按钮展开并聚焦回复框；移除评论-评审 tab 跳转联动状态
- **合并评论入口**：「评论评审」与「查看评审评论」合并为一个按钮（图标 + 评论数量徽标），点击展开卡片内评论区并聚焦回复框，再点收起
- **正文「评审」按钮可收起右栏**：右栏已处于「评审」tab 时点击正文区 `review-toggle` 按钮即收起右栏（右栏未展开时点击 = 展开并切到评审；在其他 tab 时点击 = 切到评审不收起），按钮 title 随状态提示
- **左栏/右栏收起展开动画**：宽度 0.25s ease 过渡（右栏常驻渲染，收起时宽度 0 + 裁切内容）；拖动分栏/调整左栏宽度时自动禁用过渡保证实时跟手
- **修复：边栏操作中断正文音乐**：`dangerouslySetInnerHTML` 每次渲染新建对象字面量，React 按对象引用比较导致右栏展开/收起时重写正文 innerHTML、重建音乐 iframe → 改为 useMemo 传稳定引用，正文 iframe 不再中断
- **修复：列表卡片显示音乐标记**：列表摘要/标题/导言中的媒体占位文本（如 `music163<id>/music163`、`[music 163]<id>[/music 163]`）在规整时剥离，卡片不再出现「music16…/music163」
- **互动按钮组展开/收起动画**：右下角操作按钮（投币/点赞/收藏/分享）改为常驻渲染，展开时淡入上浮且逐项错开（stagger 0/0.06/0.12/0.18s），收起时反向淡出后隐藏
- **写作首页改造**：点击左栏「写作」回到写作首页（关闭当前编辑器文档）；首页不再显示编辑器工具栏；本地存档以文章卡片展示（标题/摘要/字数/最后编辑时间 + 删除按钮），删除有二次确认（第一次点击进入确认态，3 秒未确认自动恢复；新增 `deleteLocalDoc` IPC，经 `assertInside` 路径防护）
- **写作首页卡片复用文章卡片样式**：本地存档卡片复用 `.article-card` 组件结构（title/excerpt/meta）与样式，与其他页面文章卡片视觉一致；删除按钮悬浮右上角
- **文章配图同步（.image 隐藏目录）**：拉取远端文章时把正文图片下载到本地 `.image/<cid>/` 并改写正文引用为相对路径；推送/发布时解析本地 `.image` 引用逐张 `upload/full` 上传并替换为远端 URL（单张失败整体失败，不提交半成品）；`.image` 目录不出现在文档树/首页
- **写作文件夹与文件树**：写作首页新增「新建文件夹」、文件夹可点击进入（面包屑导航、可在文件夹内新建草稿）；左栏「本地存档」显示递归文件树（文件夹可展开）；`LocalNode` 增加 `rel` 相对路径；本地树不再限两层

## v0.0.4（2026-08-09）

### 顶栏与窗口
- 取消系统标题栏（无边框窗口），顶栏自绘最小化 / 全屏 / 关闭控件，并支持双击区域拖动窗口
- 所有页面统一顶栏：显示当前页面标题（居中），阅读态左侧提供「返回列表」按钮
- 顶栏与主区平齐、不占左栏空间；去掉顶栏下边框，按钮样式统一
- 左栏空白区域与顶栏空白区域均可按住拖动窗口

### 左栏
- 五个模块（写作 / 推荐 / 连载 / 活动 / 作品库）增加 lucide 图标
- 折叠左栏（窄条仅图标），折叠按钮位于顶栏；展开时可拖动右缘调整宽度（180–380px）
- 折叠状态与宽度均 localStorage 持久化

### 阅读页右栏（目录 / 评论 / 评审一体）
- 右栏 tab 化：目录、评论、评审统一在右栏切换，宽度可拖动调整（默认 1:2 分栏）
- 评论与评审关联（接口 `reviewid` 字段）：同一评审的评论聚合分组，组头标明评判者、评分与综合评价
- 评论可「跳转到对应评审」（切评审 tab 并滚动定位）；评审可「回复评审」「查看评审评论」（双向联动）
- 评论区被引用内容单行省略；评审 tab 对本人文章隐藏
- 正文目录与评论区移入右栏，正文区只保留标题、正文与返回

### 文章页互动
- 投币 / 点赞 / 收藏 / 分享改为右下角悬浮圆形按钮组，垂直分布，「+/-」展开收起
- 新增「置顶」按钮（滚回正文顶部）；收藏改为五角星图标；投币图标更换

### 写作首页与栏目首页
- 写作首页：本地存档存在内容时展示存档列表，可直接点击打开
- 栏目首页：移除 header 容器与分割线、去掉内边距，区块标题（置顶文章 / 最新发布 / 推荐合集 等）提级为 h2

### 文章卡片与图片
- 所有文章卡片高度统一（封面固定高度 + min-height 拉齐），描述最多三行，meta 行贴底、body 占满卡片
- 图片加载失败兜底：封面显示灰底 + 占位图标，正文 / 头像等统一灰底替代浏览器破图图标

### 修复
- 修复文章正文、文章列表、编辑器无法滚动的问题（flex column 容器缺 `min-height: 0` 导致滚动区失效）
- 修复评审面板宽度不自适应右栏（百分比宽度相对错误）

## v0.0.3（2026-08-08）
v0.0.2 样式改进交付：所有排序支持倒序与按字数、评分/点赞/评论/阅读并列排名；推荐 / 连载 / 活动 / 作品库栏目首页；活动树展开与进行中 / 评审中状态标记、未完成评审任务红点；评审「我的 / 所有评审」双视图与编辑、五维平均分实时预示、态度排序；正文与评审栏拖动分栏；活动卡片统计与置顶文章。

## v0.0.2（2026-08-06）
修复 Windows 版无法打开界面的问题（Linux 交叉打包时原生模块 `better-sqlite3` 误打包为 Linux 版二进制，现自动注入 Windows 版 prebuild）；新增任意界面 Ctrl+F12 开发者工具。

## v0.0.1
首版交付：M0 三端骨架与打包、M1 写作闭环（登录 / 同步 / 编辑 / 发布 / 四态）、M2 读审一体（阅读视图 + 评审面板）、M3 内容浏览（推荐 / 连载 / 活动 / 作品库）。
